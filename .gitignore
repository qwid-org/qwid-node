# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.vscode
.idea
.DS_Store

docker

__debug_bin*
qtbox

# Compiled node binaries
/mining
/webui
/explorer
/explorer.new
/sending

# Locally built binaries that must never be committed (audit F-5)
/sendingTransaction
/genWallet
/resetWallet
# Pentest output: a list of this infrastructure's weaknesses
deploy/qwid_logs/
